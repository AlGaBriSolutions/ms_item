no tengo ni idea de como se le pega al endpoint graphql, osea no se como el gateway redirecciona ahi.
ACLARACION, el endpoint ya esta hecho en base a las especificaciones de graphql, lo que digo es que el que hace el gateway
tiene que ver como hace la redireccion de los queries.